import streamlit as st

# عنوان الصفحة
st.title("تجربة أولى مع Streamlit 🎉")

# نص بسيط
st.write("أهلاً يا مريم 🌸، جربي تكتبي أي حاجة تحت:")

# مربع إدخال
user_input = st.text_input("اكتب كلمة أو جملة:")

# زرار
if st.button("اضغط هنا"):
    st.success(f"إنت كتبتي: {user_input}")
